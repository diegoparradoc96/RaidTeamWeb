export interface IPlayerClass {
  name: string;
  spec: {
    name: string;
    image: string;
  };
}

export interface IPlayerClasses {
  name: string;
  spec: {
    name: string;
    image: string;
  }[];
}